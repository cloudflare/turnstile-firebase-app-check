{
  "name": "@cloudflare/turnstile-firebase-app-check",
  "version": "0.0.3",
  "description": "An App Check provider using Cloudflare Turnstile",
  "keywords": ["cloudflare", "turnstile", "firebase"],
  "repository": {
    "type": "git",
    "url": "https://github.com/cloudflare/turnstile-firebase-app-check"
  },
  "main": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/cloudflare-turnstile": "^0.1.3",
    "@types/node": "20.8.2",
    "gts": "^5.2.0",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "firebase": "^10.7.2"
  },
  "packageManager": "yarn@4.1.1"
}
